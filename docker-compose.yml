version: "2"
services:
  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./docker-nginx/default.conf:/etc/nginx/conf.d/default.conf
  web:
    image: react-cli
    command: npm start
    volumes:
      - ./app:/app
